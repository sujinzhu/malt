#foreach($theme in $j.Themes)
  <li><a target="_self" href="${url.for("%{area='', controller='home', action='settheme', querystring='theme=$theme.ShortName'}")}">
  <img src="${siteRoot}/Content/css/theme-roller/${theme.IconUrl}" alt="${theme.Name}" title="${theme.Name}"/>
  <span class="themeName">${theme.Name}</span></a></li>
#end
