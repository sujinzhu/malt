$(function(){ 
	/*$('#menu').tabs({
		collapsible: true,
		selected: -1,
		select: function(event, ui) {
			var url = $.data(ui.tab, 'load.tabs');
			if ( url ) {
				location.href = url;
				return false;
			}
			return true;
		}
	});
    $('#menu div button span').css('float', 'left');
    $('#pageHeader').addClass('ui-widget-header ui-corner-top');
    $('.pageBody').addClass('ui-widget-content ui-corner-bottom');*/
    fnjQuerifyInput();
    fnButtonEffect();
});
